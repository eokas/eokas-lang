
struct Object {
    var name: string;
}

struct Asset : Object {
    var path: string;
}

var asset = make Asset{
    name = "eokas",
    path = "https://asset.example.com"
};

print(asset.name);
print(asset.path);
