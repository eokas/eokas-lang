module main {
    import io : eokas.io;

    func main(args: array<string>): i32 {
        struct A {
            static var a: i32 = 6;
        }

        struct B : A {

        }

        B.a = 12;

        io.print(A.a);
        io.print(B.a);
    }
}
