

using base : "./base"

public val clamp = func(var x: f32, var a: f32, var b: f32): f32 {
    if(x < a) x = a;
    if(x > b) x = b;
    return x;
}

public val clamp01 = func(var x: f32): f32 {
    return clamp(x, 0.0, 1.0);
}

